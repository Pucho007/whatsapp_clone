import 'package:flutter/material.dart';
void main()=>runApp(CallsScreen());

class CallsScreen extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('CALLS'),
      ),
    );
  }
}